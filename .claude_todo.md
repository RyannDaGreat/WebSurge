# WebSurge — TODO

Synced with the session task list. Two-way: edit here and I will pick it up.

## Done
- [x] Scaffold dump: git init, src/, manifest, concerns
- [x] emsdk 6.0.0 via setup.sh
- [x] WASM host with the full CLAP API surface
- [x] Extract GUI layout from SkinModel.cpp
- [x] Render the real Surge GUI in the browser (canvas blit of Surge's own editor)
- [x] AudioWorklet audio path
- [x] Fix AudioWorklet unavailable over LAN (secure context / HTTPS)
- [x] Mount /SurgeXTData into both wasm modules
- [x] QWERTY note input
- [x] 128-key piano strip, full browser width, lights up when playing
- [x] Zoom + HiDPI
- [x] GitHub Pages: site live at https://ryanndagreat.github.io/WebSurge/
- [x] README with screenshot and thanks to Surge Synth Team + 37 patch authors
- [x] ALL 3559 patches — factory in the startup archive, 3rd-party fetched on click
- [x] Download progress bar
- [x] Stale patch name — root cause was that NO JUCE timer had ever fired

## In progress
- [x] 10 Tailwind skins + theme dropdown
- [x] 5 MORE skins, sleek/restrained + de-stripe Swiss
      - [x] 11 linear, 12 vercel, 13 nord, 14 mono, 15 sky (one subagent each)
      - [x] Swiss: dropped the diagonal hatch decor (now '')
      - [x] added names to ORDER, regenerated src/js/themes.js -- 15 skins
      - [x] node tools/skin_shots.mjs -- all 15 render, only the known
            /tailwindcss 404
      - [x] .frenzy/sidebar_shots.mjs (new) -- proves the SELECTED patch row
            accent compiles; skin_shots never selects anything, so the one
            element carrying each skin's accent was invisible in its output
      - [x] node .frenzy/lazy_test.mjs -- 3559 patches, 3rd-party patch loads

- [x] Fourth input mode: ryohey/signal, the real application, built by us
      - [x] Ruled out a library route by inspection: no npm package, no web
            component, no embed API, `packages/` holds no mountable editor
      - [x] `setup.sh signal` — pinned SHA, patch, `npm ci`, build, stage
      - [x] `patches/signal-embed.patch` — ParentPortOutput + cloud/analytics off
      - [x] `src/js/input/mode-signal.js` — iframe + message-to-`io` bridge
      - [x] Registered in INPUT_MODES, `Ctrl+4` binding
      - [x] 17 MB -> 2.9 MB committed; zero offsite requests
      - [x] Verified: drawn notes and playback both reach the worklet, velocity
            and delay honoured, clean teardown, other three modes unaffected

## Next
- [ ] Widen `io` with pitchBend/cc so signal's automation lanes reach the engine.
      The worklet already handles both message types (manifest §15); nothing
      sends them. This is the single biggest gap in the signal mode.
- [ ] Web MIDI input — cheap, same seam as the on-screen keyboard
- [ ] Timestamped event queue in surge-worklet.js — fixes timing for ALL THREE
      of pianoroll, notation and signal at once, and is the only way any of them
      can honestly be called a sequencer

## Backburner
- [ ] Unify the two Surge instances (blocked: needs COOP/COEP headers, which
      branch-published Pages cannot set)
- [ ] layout.json only captured PARAMETER_CONNECTED connectors; non-parameter
      connections were never recorded. Only affects test scaffolding.
