# Surge XT → WASM — Task List

Two-way synced with the session task list. Governing rule: `CLAUDE.md` —
**no quick hacks, the full thing or nothing.**

## Phase 0 — Scaffolding
- [x] `git init` the dump (was tracked by parent `/root/CleanCode` repo)
- [x] `CLAUDE.md` with the no-hacks rule
- [x] `claude_instructions.md` manifest
- [x] `concerns.md`
- [x] `.gitignore`, directory tree
- [x] `setup.sh` — emsdk 6.0.0 + Surge clone at pinned SHA

## Phase 1 — Toolchain
- [x] Install emsdk pinned to 6.0.0 (`emcc 6.0.0`, afa15e0c)
- [x] Clone Surge @ `fae324266aed52d3bd03ef2c7fb68e9098ada961` + 23 submodules
- [ ] Confirm `emcc` builds a trivial SIMD program

## Phase 2 — Engine (the hard part)
- [ ] CMake Emscripten toolchain config for `surge-common`
- [ ] Resolve 32-bit alignment failures (`QuadFilterUnit_Impl.h`, Spring Reverb)
- [ ] simde SSE → `-msimd128` mapping
- [ ] Disable/replace LuaJIT, SQLite, MTS-ESP for WASM
- [ ] Build Surge CLAP as WASM
- [ ] Our own host: full CLAP surface (params, state/patch load, notes, MIDI,
      modulation, displays) — not the reference build's 14 functions
- [ ] **Verify: render audio and inspect the samples.** Compiling proves nothing.

## Phase 3 — Audio path
- [ ] AudioWorklet processor, 128-frame quantum (Surge block = 32, divides evenly)
- [ ] Fetch wasm on main thread, transfer via `messagePort`
- [ ] Glitch-free sustained playback

## Phase 4 — GUI (the deliverable)
- [x] `tools/dump_layout.cpp` + `gen_layout.sh` — **172** connectors, 14 component
      types → `src/layout.json`. Links Surge's own registry rather than parsing
      C++, so modifiers/parent-relative coords/expanded properties are exact.
- [x] Stage the 142 classic-skin SVGs into `src/skin/` (3.2 MB, committed)
- [x] Verify all 38 referenced `BACKGROUND` ids resolve to SVGs (0 missing;
      `gen_layout.sh` now fails the build if any are absent)
- [ ] Audit: does every visible control have a connector? Find what doesn't.
- [ ] Widgets: slider, multiswitch, switch, filter selector, osc menu, FX menu,
      LFO display, VU meter, number field
- [ ] Two-way param binding to the engine
- [ ] **Verify: screenshot + VLM check against real Surge XT.**

## Phase 5 — Patches
- [ ] `tools/stage_data.sh` — copy all resource banks into `src/data/`
- [ ] `tools/gen_patch_index.py` — patch index JSON (category tree, ~2000 patches)
- [ ] `.fxp` → strip VST2 wrapper → `clap_plugin_state.load`
- [ ] Patch browser UI
- [ ] Wavetables + impulses (convolution reverb) load correctly

## Phase 6 — Keyboard
- [ ] `zxcvbnm,./` + `qwertyuiop[]\` → naturals; sharps on row above, piano-gapped
- [ ] Octave shift, velocity, no auto-repeat, no stuck notes on blur

## Open questions
- "major notes" read as *naturals/white keys*. Confirm if it meant a major scale.
- `src/data/` gitignored (regenerable). Reverse if a self-contained checkout is wanted.

## Remaining to be "completely ready and tested" (2026-08-07)

Working and browser-verified (10/10 in tools/browser_test.mjs):
- [x] Engine runs in AudioWorklet; 766 params exposed
- [x] Panel renders from Surge's own skin + connector table (131 controls)
- [x] All 3559 patches browsable; patch load verified end to end
- [x] QWERTY note on/off verified in a real browser

Still missing before this counts as done:
- [ ] **LFO display (CLFOGui)** — the orange region. Surge draws the LFO
      waveform in code; needs a canvas widget.
- [ ] **Modulation matrix** — the blue region. Routing grid, also code-drawn.
- [ ] **Oscillator display** — `osc.display`, the waveform preview.
- [ ] **Scene A/B + LFO selection** — one widget per connector currently always
      binds scene A / LFO 1. Selecting a scene must retarget the shared controls.
- [ ] **Wavetables from disk** — patches referencing a wavetable need
      /SurgeXTData mounted; currently only the patch .fxp is fed in.
- [ ] **Audible check in a real browser with speakers** — headless Chrome has no
      audio device, so browser audio is currently verified only by asserting on
      the control messages plus the headless node render.
- [ ] Menus (osc type, FX type) are readouts, not dropdowns.
